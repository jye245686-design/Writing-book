# DeepSeek API Key（必填，用于书名/大纲/正文等 AI 生成）
# 获取地址：https://platform.deepseek.com/
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# 模型（可选）。默认 deepseek-chat（非思考）；可改为 deepseek-reasoner（思考模式）。后续支持界面选择。
# DEEPSEEK_MODEL=deepseek-chat

# 后端服务端口（可选，默认 3002）
# PORT=3002

# 用户认证（可选）。生产环境务必设置 JWT_SECRET 为随机长字符串
# JWT_SECRET=your_random_secret_here
# JWT_EXPIRES_IN=7d

# 短信验证码（可选）。配置后登录时将发送真实短信；未配置时仅开发环境在控制台打印验证码
# 腾讯云短信：https://console.cloud.tencent.com/smsv2 开通服务，创建应用得 SdkAppId，申请国内短信签名与正文模板（验证码模板变量为 {1}）
# TENCENT_SECRET_ID=你的 SecretId
# TENCENT_SECRET_KEY=你的 SecretKey
# TENCENT_SMS_SDK_APP_ID=1400xxxxxx
# TENCENT_SMS_SIGN_NAME=你的短信签名内容
# TENCENT_SMS_TEMPLATE_ID=12xxxx

# 生产环境单体部署时，后端会托管 web/dist；若前端构建产物不在默认路径，可指定：
# PUBLIC_DIR=/absolute/path/to/web/dist

# MySQL 数据库（可选）。配置后用户、验证码、项目数据将存入 MySQL；未配置时使用 server/data/*.json 文件
# ---------- 优先：国内云数据库（腾讯云 / 阿里云 / 华为云 MySQL） ----------
# 在云控制台购买/开通 MySQL 后，在「实例」或「数据库连接」中查看内网/公网地址、端口、账号、密码，并创建库名（如 writing_book）。
# 方式 A：分项填写（推荐）
# MYSQL_HOST=你的实例地址（如 sh-xxx.sql.tencentcdb.com 或 rm-xxx.mysql.rds.aliyuncs.com）
# MYSQL_PORT=3306
# MYSQL_USER=数据库账号
# MYSQL_PASSWORD=数据库密码
# MYSQL_DATABASE=writing_book
# 方式 B：连接串（把上面信息拼成 URL）
# DATABASE_URL=mysql://用户:密码@主机:3306/writing_book
# 若云厂商要求 SSL 且报错，可试：MYSQL_SSL=0 或联系厂商开启非 SSL 访问
# ---------- Zeabur 同项目 MySQL（内网） ----------
# MYSQL_HOST=mysql.zeabur.internal
# MYSQL_PORT=3306
# MYSQL_USER=...
# MYSQL_PASSWORD=...
# MYSQL_DATABASE=writing_book
# ---------- 海外可选：PlanetScale ----------
# DATABASE_URL=mysql://xxx:yyy@xxx.connect.psdb.cloud/库名?sslaccept=strict
